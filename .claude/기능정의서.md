# Knowledge Quiz 프론트엔드 기능 정의서

## 문서 정보
- **작성일**: 2026-01-08
- **버전**: v1.0
- **작성자**: Claude
- **프로젝트**: Knowledge Quiz (상식 퀴즈 앱)

---

## 1. 프로젝트 개요

### 1.1 기술 스택
- **프레임워크**: React 18.3.1 + TypeScript
- **빌드 도구**: Vite 6.3.5
- **스타일링**: TailwindCSS + class-variance-authority
- **UI 컴포넌트**: Radix UI (shadcn/ui 기반)
- **아이콘**: Lucide React
- **HTTP 클라이언트**: Axios 1.13.2
- **라우팅**: React Router DOM 7.12.0
- **폼 관리**: React Hook Form 7.55.0
- **상태 관리**: React Hooks (useState, useEffect) + LocalStorage

### 1.2 프로젝트 구조
```
frontend/
├── src/
│   ├── components/
│   │   ├── Home.tsx                    # 메인 화면
│   │   ├── Auth.tsx                    # 로그인/회원가입
│   │   ├── QuizSelection.tsx           # 퀴즈 선택
│   │   ├── QuizPlay.tsx                # 퀴즈 풀이
│   │   ├── QuizResult.tsx              # 결과 화면
│   │   ├── MyPage.tsx                  # 마이페이지
│   │   ├── figma/
│   │   │   └── ImageWithFallback.tsx
│   │   └── ui/                         # Radix UI 컴포넌트들
│   ├── App.tsx                         # 메인 앱 컴포넌트
│   └── main.tsx                        # 엔트리 포인트
├── public/
├── package.json
├── vite.config.ts
└── index.html
```

---

## 2. 데이터 모델

### 2.1 User (사용자)
```typescript
interface User {
  id: string;           // 사용자 고유 ID
  username: string;     // 사용자 이름
  email: string;        // 이메일
}
```

### 2.2 Quiz (퀴즈)
```typescript
interface Quiz {
  id: string;                           // 퀴즈 고유 ID
  title: string;                        // 퀴즈 제목
  category: string;                     // 카테고리 (history, science, etc.)
  difficulty: 'easy' | 'medium' | 'hard';  // 난이도
  questions: Question[];                // 문제 목록
}
```

### 2.3 Question (문제)
```typescript
interface Question {
  id: string;           // 문제 고유 ID
  question: string;     // 문제 텍스트
  options: string[];    // 선택지 배열 (4개)
  correctAnswer: number;  // 정답 인덱스 (0-3)
}
```

### 2.4 QuizHistory (퀴즈 기록)
```typescript
interface QuizHistory {
  id: string;           // 기록 고유 ID
  quizId: string;       // 퀴즈 ID
  quizTitle: string;    // 퀴즈 제목
  category: string;     // 카테고리
  score: number;        // 맞은 개수
  totalQuestions: number;  // 전체 문제 수
  completedAt: Date;    // 완료 시간
  timeTaken: number;    // 소요 시간(초)
}
```

---

## 3. 화면별 기능 정의

### 3.1 Home (메인 화면)
**파일**: `src/components/Home.tsx`

#### 기능
- 애플리케이션 랜딩 페이지
- 로그인 전/후 다른 UI 표시
- 퀴즈 앱 소개 및 주요 기능 안내

#### UI 구성
1. **헤더**
   - 로고: "퀴즈 마스터" + 트로피 아이콘
   - 로그인 전: "로그인" 버튼
   - 로그인 후: 사용자 이름, 마이페이지 버튼, 로그아웃 버튼

2. **히어로 섹션**
   - 메인 타이틀: "퀴즈로 배우는 즐거움"
   - 서브 타이틀: "다양한 카테고리의 퀴즈를 풀며 지식을 넓혀보세요"
   - CTA 버튼:
     - "퀴즈 시작하기": 로그인 시 퀴즈 선택 화면으로, 미로그인 시 로그인 화면으로
     - "회원가입" (미로그인 시만 표시)

3. **특징 카드 (3개)**
   - 📚 다양한 카테고리: 역사, 과학, 문화 등 다양한 주제의 퀴즈
   - 🎯 난이도 선택: 초급부터 고급까지 실력에 맞게 선택
   - 📊 성적 추적: 내 퀴즈 기록과 통계를 확인

4. **푸터**
   - 저작권 정보: "© 2026 퀴즈 마스터. All rights reserved."

#### 주요 Props
```typescript
interface HomeProps {
  user: User | null;
  onNavigate: (screen: Screen) => void;
  onLogout: () => void;
}
```

#### 사용자 플로우
1. 사용자가 메인 화면 접속
2. "퀴즈 시작하기" 클릭
   - 로그인 상태: QuizSelection 화면으로 이동
   - 미로그인 상태: Auth 화면으로 이동
3. "회원가입" 클릭 (미로그인 시): Auth 화면으로 이동
4. 로그인 상태에서 사용자 이름 클릭: MyPage 화면으로 이동
5. "로그아웃" 클릭: 로그아웃 처리 및 홈 화면 유지

---

### 3.2 Auth (로그인/회원가입)
**파일**: `src/components/Auth.tsx`

#### 기능
- 이메일 기반 로그인 및 회원가입
- 현재는 Mock 구현 (실제 백엔드 연동 필요)
- 로그인 정보 localStorage 저장

#### UI 구성
1. **헤더**
   - "돌아가기" 버튼 (Home으로)

2. **인증 폼**
   - 로그인/회원가입 토글
   - 회원가입 시 추가 필드: 사용자 이름
   - 공통 필드: 이메일, 비밀번호
   - 에러 메시지 표시 영역
   - 제출 버튼
   - 로그인/회원가입 전환 링크

#### 입력 검증
- **로그인**
  - 모든 필드 필수 입력

- **회원가입**
  - 모든 필드 필수 입력
  - 비밀번호 6자 이상

#### Mock 로직
- 로그인: 이메일과 비밀번호 입력 시 자동 로그인 (검증 없음)
  - username은 이메일 @ 앞부분으로 자동 설정
- 회원가입: 모든 정보 입력 시 자동 가입 및 로그인

#### 주요 Props
```typescript
interface AuthProps {
  onLogin: (user: User) => void;
  onBack: () => void;
}
```

#### 상태 관리
```typescript
const [isLogin, setIsLogin] = useState(true);  // 로그인/회원가입 토글
const [email, setEmail] = useState('');
const [password, setPassword] = useState('');
const [username, setUsername] = useState('');
const [error, setError] = useState('');
```

#### 향후 개선 사항
- 백엔드 API 연동
- JWT 토큰 기반 인증
- 이메일 인증
- 비밀번호 찾기
- 소셜 로그인 (Google, Kakao 등)

---

### 3.3 QuizSelection (퀴즈 선택)
**파일**: `src/components/QuizSelection.tsx`

#### 기능
- 카테고리별 퀴즈 목록 표시
- 카테고리 및 난이도 필터링
- 퀴즈 정보 미리보기 (제목, 카테고리, 난이도, 문제 수)

#### 카테고리 목록
```typescript
const categories = [
  { id: 'history', name: '역사', icon: BookOpen, color: 'bg-amber-500' },
  { id: 'science', name: '과학', icon: Beaker, color: 'bg-blue-500' },
  { id: 'geography', name: '지리', icon: Globe, color: 'bg-green-500' },
  { id: 'culture', name: '문화', icon: Music, color: 'bg-purple-500' },
  { id: 'entertainment', name: '엔터테인먼트', icon: Film, color: 'bg-pink-500' },
  { id: 'technology', name: '기술', icon: Code, color: 'bg-indigo-500' },
  { id: 'general', name: '일반상식', icon: Brain, color: 'bg-teal-500' }
];
```

#### UI 구성
1. **헤더**
   - "홈으로" 버튼

2. **타이틀 섹션**
   - "퀴즈 선택"
   - 설명: "원하는 카테고리와 난이도를 선택하세요"

3. **카테고리 필터**
   - 전체 + 7개 카테고리 버튼
   - 아이콘 + 카테고리명 표시
   - 선택된 카테고리 강조 표시

4. **난이도 필터**
   - 전체, 초급, 중급, 고급 버튼
   - 선택된 난이도 강조 표시

5. **퀴즈 목록 (그리드)**
   - 카드 형태로 표시
   - 각 카드 정보:
     - 카테고리 색상 헤더 + 아이콘
     - 퀴즈 제목
     - 난이도 뱃지
     - 카테고리명
     - 문제 수
     - "시작하기 →" 링크

#### Mock 퀴즈 데이터
현재 6개의 샘플 퀴즈 제공:
1. **한국사 기초** (역사/초급) - 5문제
2. **세계사 도전** (역사/중급) - 5문제
3. **기초 과학** (과학/초급) - 5문제
4. **세계 지리** (지리/중급) - 5문제
5. **K-POP 퀴즈** (엔터테인먼트/초급) - 5문제
6. **프로그래밍 기초** (기술/중급) - 5문제

#### 필터링 로직
```typescript
const filteredQuizzes = mockQuizzes.filter(quiz => {
  const categoryMatch = selectedCategory === 'all' || quiz.category === selectedCategory;
  const difficultyMatch = selectedDifficulty === 'all' || quiz.difficulty === selectedDifficulty;
  return categoryMatch && difficultyMatch;
});
```

#### 주요 Props
```typescript
interface QuizSelectionProps {
  user: User | null;
  onQuizSelect: (quiz: Quiz) => void;
  onBack: () => void;
}
```

#### 향후 개선 사항
- 백엔드 API에서 퀴즈 목록 가져오기
- 페이지네이션
- 검색 기능
- 퀴즈 미리보기 모달
- 인기 퀴즈 표시
- 최근 풀었던 퀴즈 표시

---

### 3.4 QuizPlay (퀴즈 풀이)
**파일**: `src/components/QuizPlay.tsx`

#### 기능
- 문제별 순차 진행
- 타이머 기능 (경과 시간 표시)
- 진행률 표시
- 이전 문제로 돌아가기
- 답안 선택 및 제출

#### UI 구성
1. **헤더**
   - 퀴즈 제목
   - 현재 문제 번호 / 전체 문제 수
   - 경과 시간 (MM:SS 형식)
   - 종료(X) 버튼
   - 진행률 프로그레스 바

2. **문제 영역**
   - Question 번호 뱃지
   - 문제 텍스트 (큰 폰트)
   - 4개 선택지 (A, B, C, D)
     - 선택 전: 회색 테두리
     - 선택 후: 보라색 강조
   - 네비게이션 버튼:
     - "이전" (첫 문제에서는 비활성화)
     - "다음" / "완료" (답 선택 전 비활성화)

3. **완료 화면**
   - 체크 아이콘 + "퀴즈 완료!"
   - 로딩 스피너
   - "결과를 집계하고 있습니다..." 메시지
   - 1.5초 후 자동으로 결과 화면으로 이동

#### 상태 관리
```typescript
const [currentQuestionIndex, setCurrentQuestionIndex] = useState(0);
const [selectedAnswer, setSelectedAnswer] = useState<number | null>(null);
const [answers, setAnswers] = useState<(number | null)[]>(Array(quiz.questions.length).fill(null));
const [showResult, setShowResult] = useState(false);
const [startTime] = useState(Date.now());
const [timeElapsed, setTimeElapsed] = useState(0);
```

#### 타이머 로직
- 1초마다 경과 시간 업데이트
- 컴포넌트 언마운트 시 타이머 정리

#### 점수 계산
```typescript
const score = answers.reduce((acc, answer, idx) => {
  return acc + (answer === quiz.questions[idx].correctAnswer ? 1 : 0);
}, 0);
```

#### 주요 Props
```typescript
interface QuizPlayProps {
  quiz: Quiz;
  onComplete: (score: number, total: number, timeTaken: number) => void;
  onQuit: () => void;
}
```

#### 사용자 플로우
1. 첫 문제 표시
2. 답 선택 → "다음" 버튼 활성화
3. "다음" 클릭 → 다음 문제로 이동
4. 마지막 문제에서 "완료" 클릭
5. 완료 화면 1.5초 표시
6. 결과 화면으로 자동 이동

#### 향후 개선 사항
- 제한 시간 모드
- 힌트 기능
- 문제 북마크
- 즉시 정답/오답 피드백
- 해설 표시

---

### 3.5 QuizResult (결과 화면)
**파일**: `src/components/QuizResult.tsx`

#### 기능
- 퀴즈 결과 요약 표시
- 성과 레벨별 피드백
- 상세 통계 제공
- 결과 공유 기능

#### UI 구성
1. **헤더 (성과별 색상)**
   - 이모지 (90%+: 🎉, 70%+: 🌟, 50%+: 👍, 50% 미만: 💪)
   - 성과 메시지 ("완벽해요!", "훌륭해요!", "잘했어요!", "다시 도전해보세요!")
   - 퀴즈 제목

2. **점수 원형 그래프**
   - SVG 원형 프로그레스 바
   - 중앙에 정답률 표시 (큰 폰트)
   - 애니메이션 효과

3. **통계 카드 (3개)**
   - 🏆 정답: 맞은 문제 수
   - 🎯 전체: 전체 문제 수
   - ⏱️ 소요시간: 분:초 형식

4. **결과 요약**
   - 맞은 문제 개수 (초록색)
   - 틀린 문제 개수 (빨간색)
   - 평균 소요 시간/문제

5. **액션 버튼**
   - "다른 퀴즈 풀기": QuizSelection 화면으로
   - "홈으로": Home 화면으로
   - "결과 공유하기": 클립보드 복사 또는 Web Share API

#### 성과 레벨 기준
```typescript
90% 이상: "완벽해요!" 🎉 (초록색)
70-89%: "훌륭해요!" 🌟 (파란색)
50-69%: "잘했어요!" 👍 (노란색)
50% 미만: "다시 도전해보세요!" 💪 (주황색)
```

#### 공유 기능
- 공유 텍스트: "{퀴즈 제목} 퀴즈에서 {정답률}점을 받았어요! 🎉"
- Web Share API 지원 브라우저: 네이티브 공유
- 미지원 브라우저: 클립보드 복사 + 알림

#### 주요 Props
```typescript
interface QuizResultProps {
  score: number;
  total: number;
  timeTaken: number;
  quizTitle: string;
  onPlayAgain: () => void;
  onGoHome: () => void;
}
```

#### 향후 개선 사항
- 문제별 정답/오답 상세 보기
- 틀린 문제 다시 풀기
- SNS 공유 (Twitter, Facebook, Kakao)
- 리더보드 표시
- 뱃지/업적 시스템

---

### 3.6 MyPage (마이페이지)
**파일**: `src/components/MyPage.tsx`

#### 기능
- 사용자 프로필 표시
- 전체 통계 요약
- 카테고리별 성적 분석
- 퀴즈 히스토리 조회

#### UI 구성
1. **헤더**
   - "홈으로" 버튼

2. **사용자 프로필 카드 (그라데이션)**
   - 프로필 아이콘 (👤)
   - 사용자 이름
   - 이메일
   - 4개 통계 카드:
     - 완료한 퀴즈 수
     - 평균 정답률
     - 맞은 문제 수
     - 총 소요시간

3. **좌측 사이드바 (전체 통계)**
   - **전체 통계**
     - 🏆 평균 점수
     - 🎯 총 문제 수
     - ⏱️ 평균 시간

   - **카테고리별 성적**
     - 카테고리명
     - 정답률 프로그레스 바
     - 퀴즈 수 / 정답 수 표시

4. **우측 메인 영역 (퀴즈 히스토리)**
   - 📅 퀴즈 기록 헤더
   - 히스토리 카드 리스트:
     - 퀴즈 제목
     - 카테고리 뱃지
     - 소요 시간
     - 완료 날짜
     - 정답률 (점수별 색상)
     - 맞은 문제 / 전체 문제

#### 통계 계산 로직
```typescript
// 전체 통계
const totalQuizzes = history.length;
const totalQuestions = history.reduce((sum, quiz) => sum + quiz.totalQuestions, 0);
const totalCorrect = history.reduce((sum, quiz) => sum + quiz.score, 0);
const averageScore = Math.round((totalCorrect / totalQuestions) * 100);
const totalTimeTaken = history.reduce((sum, quiz) => sum + quiz.timeTaken, 0);

// 카테고리별 통계
const categoryStats = history.reduce((acc, quiz) => {
  if (!acc[quiz.category]) {
    acc[quiz.category] = { count: 0, correct: 0, total: 0 };
  }
  acc[quiz.category].count++;
  acc[quiz.category].correct += quiz.score;
  acc[quiz.category].total += quiz.totalQuestions;
  return acc;
}, {});
```

#### 점수별 색상
```typescript
80% 이상: 초록색
60-79%: 파란색
40-59%: 노란색
40% 미만: 빨간색
```

#### 주요 Props
```typescript
interface MyPageProps {
  user: User | null;
  history: QuizHistory[];
  onBack: () => void;
}
```

#### 빈 상태 처리
- 퀴즈 히스토리가 없을 때:
  - 📝 이모지
  - "아직 완료한 퀴즈가 없습니다"
  - "첫 퀴즈를 풀어보세요!"

#### 향후 개선 사항
- 기간별 필터 (일/주/월)
- 통계 그래프 (recharts 활용)
- 목표 설정 기능
- 학습 스트릭 (연속 학습 일수)
- 친구 비교 기능
- 프로필 편집

---

## 4. 전역 상태 관리

### 4.1 App.tsx 상태
```typescript
const [currentScreen, setCurrentScreen] = useState<Screen>('home');
const [user, setUser] = useState<User | null>(null);
const [selectedQuiz, setSelectedQuiz] = useState<Quiz | null>(null);
const [quizResult, setQuizResult] = useState<{...} | null>(null);
const [quizHistory, setQuizHistory] = useState<QuizHistory[]>([]);
```

### 4.2 LocalStorage 사용
- **저장 데이터**:
  - `quizAppUser`: 사용자 정보
  - `quizAppHistory`: 퀴즈 히스토리

- **로드 시점**: 앱 마운트 시 (useEffect)
- **저장 시점**:
  - 로그인/회원가입 시
  - 퀴즈 완료 시

### 4.3 화면 전환 (Screen Type)
```typescript
type Screen = 'home' | 'auth' | 'quiz-selection' | 'quiz-play' | 'quiz-result' | 'mypage';
```

---

## 5. API 연동 (향후 구현)

### 5.1 필요한 API 엔드포인트

#### 인증 API
- `POST /api/auth/signup`: 회원가입
- `POST /api/auth/login`: 로그인
- `POST /api/auth/logout`: 로그아웃
- `GET /api/auth/me`: 현재 사용자 정보

#### 퀴즈 API
- `GET /api/quizzes`: 퀴즈 목록 조회
- `GET /api/quizzes/:id`: 퀴즈 상세 조회
- `GET /api/quizzes?category={category}`: 카테고리별 퀴즈
- `GET /api/quizzes?difficulty={difficulty}`: 난이도별 퀴즈

#### 퀴즈 제출 API
- `POST /api/quiz-attempts`: 퀴즈 제출 및 채점
  ```json
  {
    "quizId": "string",
    "answers": [0, 2, 1, 3, 0],
    "timeTaken": 180
  }
  ```

#### 사용자 통계 API
- `GET /api/users/:id/history`: 퀴즈 히스토리
- `GET /api/users/:id/stats`: 사용자 통계

### 5.2 Axios 설정
```typescript
// src/api/axios.ts
import axios from 'axios';

const api = axios.create({
  baseURL: '/api',  // Vite proxy 사용
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
});

// Request interceptor (토큰 추가)
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  error => Promise.reject(error)
);

// Response interceptor (에러 처리)
api.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 로그아웃 처리
    }
    return Promise.reject(error);
  }
);

export default api;
```

---

## 6. 스타일링 및 디자인 시스템

### 6.1 컬러 팔레트
- **Primary**: Purple (보라색)
  - `purple-50` ~ `purple-700`
- **Secondary**: Blue (파란색)
- **Success**: Green (초록색)
- **Warning**: Yellow (노란색)
- **Error**: Red (빨간색)
- **Neutral**: Gray (회색)

### 6.2 반응형 브레이크포인트
```css
sm: 640px
md: 768px
lg: 1024px
xl: 1280px
2xl: 1536px
```

### 6.3 주요 UI 컴포넌트
- Button: 다양한 variant (primary, secondary, outline, ghost)
- Card: 그림자와 호버 효과
- Badge: 난이도, 카테고리 표시
- Progress: 진행률 표시
- Input: 폼 입력 필드
- Dialog/Modal: 알림 및 확인

### 6.4 애니메이션
- Hover 효과: `transition-all`, `transform hover:scale-105`
- 로딩: `animate-spin`
- 프로그레스: `transition-all duration-300`

---

## 7. 접근성 (Accessibility)

### 7.1 적용된 접근성 기능
- **Semantic HTML**: header, main, footer 사용
- **ARIA Labels**: 버튼에 title 속성
- **Keyboard Navigation**: 모든 인터랙션 요소 키보드 접근 가능
- **Focus States**: 포커스 시 outline 표시
- **Color Contrast**: WCAG AA 기준 준수

### 7.2 향후 개선 사항
- Screen reader 지원 강화
- ARIA live regions (동적 콘텐츠 알림)
- Skip to content 링크
- 키보드 단축키

---

## 8. 성능 최적화

### 8.1 현재 구현
- React.memo 사용 가능
- 이미지 지연 로딩 (ImageWithFallback 컴포넌트)
- 코드 스플리팅 (Vite 자동 처리)

### 8.2 향후 개선 사항
- Virtual scrolling (긴 목록)
- 이미지 최적화 (WebP, lazy loading)
- Service Worker (오프라인 지원)
- 캐싱 전략
- Bundle size 최적화

---

## 9. 테스트 전략 (향후 구현)

### 9.1 단위 테스트
- Vitest + React Testing Library
- 컴포넌트별 렌더링 테스트
- 이벤트 핸들러 테스트
- 유틸리티 함수 테스트

### 9.2 통합 테스트
- 사용자 플로우 테스트
- API 모킹 (MSW)

### 9.3 E2E 테스트
- Playwright 또는 Cypress
- 주요 사용자 시나리오 테스트

---

## 10. 배포 전략

### 10.1 빌드 설정
```bash
npm run build
# 결과물: dist/ 폴더
```

### 10.2 배포 옵션
1. **Vercel** (추천)
   - 자동 배포 (Git 연동)
   - Edge Functions 지원
   - 무료 티어 제공

2. **AWS S3 + CloudFront**
   - S3에 정적 파일 호스팅
   - CloudFront CDN 사용
   - Route 53으로 도메인 연결

3. **Netlify**
   - 간편한 배포
   - Form 기능 지원
   - 무료 티어 제공

### 10.3 환경 변수
```env
VITE_API_URL=https://api.example.com
VITE_APP_NAME=Knowledge Quiz
```

---

## 11. 개발 가이드

### 11.1 로컬 개발 실행
```bash
cd frontend
npm install
npm run dev
# http://localhost:5173
```

### 11.2 코드 스타일
- ESLint + Prettier 설정 필요
- TypeScript strict mode 권장
- 컴포넌트 네이밍: PascalCase
- 파일 네이밍: PascalCase (컴포넌트), camelCase (유틸)

### 11.3 커밋 컨벤션
```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅
refactor: 코드 리팩토링
test: 테스트 코드 추가
chore: 빌드 설정, 패키지 업데이트
```

---

## 12. 향후 개발 로드맵

### Phase 1: 백엔드 연동 (2주)
- [ ] API 클라이언트 구현
- [ ] 인증 API 연동
- [ ] 퀴즈 API 연동
- [ ] 에러 처리 및 로딩 상태

### Phase 2: 기능 개선 (2주)
- [ ] 검색 기능
- [ ] 페이지네이션
- [ ] 퀴즈 미리보기
- [ ] 정답/오답 해설

### Phase 3: 고급 기능 (3주)
- [ ] 리더보드
- [ ] 뱃지 시스템
- [ ] 친구 초대
- [ ] 학습 통계 그래프

### Phase 4: 최적화 및 배포 (1주)
- [ ] 성능 최적화
- [ ] SEO 최적화
- [ ] 테스트 코드 작성
- [ ] 프로덕션 배포

---

## 13. 참고 자료

### 13.1 사용된 라이브러리 문서
- [React](https://react.dev/)
- [Vite](https://vitejs.dev/)
- [TailwindCSS](https://tailwindcss.com/)
- [Radix UI](https://www.radix-ui.com/)
- [Lucide Icons](https://lucide.dev/)

### 13.2 디자인 참고
- [Figma 디자인 파일](https://www.figma.com/) (향후 추가)
- shadcn/ui: https://ui.shadcn.com/

---

## 부록 A: Mock 데이터 상세

### A.1 샘플 퀴즈 목록

#### 1. 한국사 기초 (역사/초급)
1. 고려를 건국한 인물은? → 왕건
2. 조선의 수도는? → 한양
3. 훈민정음을 창제한 왕은? → 세종
4. 임진왜란이 일어난 연도는? → 1592년
5. 3.1 운동이 일어난 연도는? → 1919년

#### 2. 세계사 도전 (역사/중급)
1. 프랑스 혁명이 시작된 연도는? → 1789년
2. 로마 제국의 초대 황제는? → 아우구스투스
3. 제2차 세계대전이 끝난 연도는? → 1945년
4. 베를린 장벽이 무너진 연도는? → 1989년
5. 마젤란이 세계 일주를 완성한 세기는? → 16세기

#### 3. 기초 과학 (과학/초급)
1. 물의 화학식은? → H2O
2. 지구에서 가장 가까운 별은? → 태양
3. 광합성을 하는 식물의 부위는? → 잎
4. 소리의 속도는 약 얼마인가? → 340m/s
5. 인체에서 가장 큰 장기는? → 피부

#### 4. 세계 지리 (지리/중급)
1. 세계에서 가장 높은 산은? → 에베레스트
2. 나일강이 흐르는 대륙은? → 아프리카
3. 캐나다의 수도는? → 오타와
4. 사하라 사막이 위치한 곳은? → 아프리카
5. 세계에서 가장 큰 섬은? → 그린란드

#### 5. K-POP 퀴즈 (엔터테인먼트/초급)
1. BTS의 데뷔곡은? → No More Dream
2. BLACKPINK의 멤버가 아닌 사람은? → 아이린
3. PSY의 대표곡은? → Gangnam Style
4. EXO가 소속된 기획사는? → SM
5. NewJeans의 데뷔 연도는? → 2022년

#### 6. 프로그래밍 기초 (기술/중급)
1. HTML의 정식 명칭은? → Hyper Text Markup Language
2. JavaScript를 만든 사람은? → 브렌던 아이크
3. Git에서 변경사항을 저장하는 명령어는? → git commit
4. CSS에서 색상을 지정하는 방법이 아닌 것은? → XYZ
5. React는 어느 회사에서 만들었나? → Facebook

---

## 문서 버전 히스토리

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.0 | 2026-01-08 | Claude | 초기 작성 - 프론트엔드 기능 전체 정의 |

---

**문서 끝**
