spring:
  application:
    name: quiz
  datasource:
    url: jdbc:mysql://localhost:3306/quiz_db
    username: root
    password: your-password
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true